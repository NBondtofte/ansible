---
- name: Verify interface status on Cisco switches
  hosts: Cisco
  gather_facts: no
  tasks:
    - name: Show interfaces status
      ios_command:
        commands:
          - show ip interface brief
          - show run
          - show version
